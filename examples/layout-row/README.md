# Layout Row Example

Focused mini-LHT example for the first layout renderer pass.

It stays inside the current mini dictionary and exercises:

- multiple `row` containers;
- fixed-width `block` elements;
- horizontal `spacer` elements;
- vertical `spacer` elements;
- text wrapping in a narrow block;
- a full-width block without explicit width.

