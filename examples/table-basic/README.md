# Table Basic Example

Focused fixture for the first table slice:

- `table`, `tr`, and `td` DOM tokens
- fixed equal-width columns in the current renderer
- per-cell background, border, padding, and inline text
- class-based table cell styling through the existing STYLE stream
