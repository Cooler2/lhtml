# Font Basic Example

Focused mini-LHT example for semantic font family and font size attributes.

It exercises:

- inherited `font` on `body`;
- block-level `fontSize`;
- inline `span fontSize`;
- semantic families `sans`, `serif`, and `mono`.
- source shorthand such as `font=serif,normal`, normalized to
  `fontFace=serif` and `fontSize=normal`.
