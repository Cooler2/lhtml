# Class Basic Example

Focused fixture for the `class` attribute and renderer resolver groundwork.

This slice preserves class names through parse, token dump, binary encode/decode,
and render traversal. Class declarations are intentionally not parsed yet.
