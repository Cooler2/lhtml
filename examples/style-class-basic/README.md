# Style Class Basic Example

Focused fixture for the first STYLE stream slice:

- source `<style>` class declarations
- binary `STYLE_BEGIN`
- shared ATTR/VALUE dictionaries in STYLE and DOM streams
- class resolution before rendering
